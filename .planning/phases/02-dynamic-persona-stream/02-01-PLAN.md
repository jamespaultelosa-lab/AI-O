---
phase: 02-dynamic-persona-stream
plan: 01
type: execute
wave: 1
depends_on: ["01-ipc-discernment"]
files_modified:
  - .agents/brain_orchestrator.cjs
  - .agents/codex_brain_pool.cjs
  - tests/Node/brain_orchestrator.test.cjs
autonomous: true
requirements: ["CHAT-02"]
---

<objective>
Route each message to one appropriate brain by default, and run a bounded multi-brain consultation only for heavy tasks.

Purpose: Make ordinary conversation fast and coherent while retaining genuinely useful collaborative deliberation for complex work.
</objective>

<tasks>

<task type="tracer">
  <name>Task 1: Add deterministic lead-brain and heavy-task routing</name>
  <files>.agents/brain_orchestrator.cjs, tests/Node/brain_orchestrator.test.cjs</files>
  <action>Replace the current all-brains default with a pure routing function that returns `{ lead, consultants, tier }`. Use a narrow ordered policy: Security for authentication, permissions, payments, secrets, data exposure, encryption, uploads, or security review; Architect for architecture, schema/database design, services, dependencies, system design, or refactors; Junior Dev for focused UI/UX/layout/component work; Senior Dev for implementation, debugging, tests, and all remaining ordinary requests. Treat a task as heavy only when it has an explicit multi-domain trigger, two or more high-impact categories, or a long/complex request. Normal and casual inputs must invoke exactly one lead brain.</action>
  <verify><automated>node --test tests/Node/brain_orchestrator.test.cjs</automated></verify>
  <done>Routing is deterministic, testable, and returns exactly one brain for normal messages.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded consultation and one final response</name>
  <files>.agents/brain_orchestrator.cjs, .agents/codex_brain_pool.cjs, tests/Node/brain_orchestrator.test.cjs</files>
  <action>For heavy tasks, query the relevant consultants sequentially using the existing persistent Codex threads. Pass each consultant's concise finding to the lead brain in a final synthesis turn. Broadcast optional consultant findings as labelled consultation updates, then broadcast only the lead's final user-facing answer. Do not emit canned fallback text. A failed model turn leaves that brain in `thinking` with no fabricated message; do not set unrelated brains idle. Keep responses concise and cap consultation count at three. Preserve each persistent thread's persona and vault context.</action>
  <verify><automated>node --test tests/Node/brain_orchestrator.test.cjs</automated></verify>
  <done>Heavy tasks exhibit relevant, finite collaboration and finish with one lead response; routine tasks retain one response.</done>
</task>

</tasks>

<success_criteria>
  <criterion>Ordinary messages result in exactly one model turn and one visible brain reply.</criterion>
  <criterion>Security, architecture, implementation, and UI requests select their respective lead brain.</criterion>
  <criterion>Heavy multi-domain requests receive bounded consultant input and one lead synthesis.</criterion>
  <criterion>No canned fallback or false idle status is emitted when a model turn fails.</criterion>
</success_criteria>
