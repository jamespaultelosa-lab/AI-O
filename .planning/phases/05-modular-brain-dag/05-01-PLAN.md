---
phase: 05-modular-brain-dag
plan: 01
type: execute
wave: 1
depends_on: ["04-conversation-navigation-header-polish"]
files_modified:
  - .agents/core/kernel.cjs
  - .agents/core/event_bus.cjs
  - .agents/core/state_machine.cjs
  - .agents/registry/brain_registry.cjs
  - .agents/registry/skill_registry.cjs
  - .agents/dag/task_graph.cjs
  - .agents/dag/graph_planner.cjs
  - .agents/dag/graph_executor.cjs
  - .agents/adapters/codex_pool_adapter.cjs
  - .agents/adapters/storage_adapter.cjs
  - .agents/brains/architect.agent.md
  - .agents/brains/senior_dev.agent.md
  - .agents/brains/junior_dev.agent.md
  - .agents/brains/security.agent.md
  - .agents/brain_orchestrator.cjs
  - .agents/speak.cjs
  - .agents/stream_thoughts.js
  - .agents/finish_task.js
  - .agents/task_watcher.cjs
autonomous: true
requirements: ["MOD-01", "MOD-02", "MOD-03", "MOD-04", "MOD-05"]
user_setup:
  - "Ensure Laravel dev server and Reverb are running if testing live UI websocket broadcasts."
estimate:
  tokens: 18000
  raw_tokens: 9000
  tasks: 5
  confidence: high
must_haves:
  truths:
    - "Brain Registry auto-discovers all brains from .agents/brains/ without hardcoded brain arrays or static regexes."
    - "Dynamic DAG planner decomposes tasks into structured dependency nodes and hands off execution between brains."
    - "Event Bus broadcasts thought streams, status updates, and stage transitions asynchronously to Laravel webhooks and IPC files."
    - "Legacy scripts (speak.cjs, stream_thoughts.js, finish_task.js, task_watcher.cjs) remain 100% functional through backward-compatible facade shims."
    - "Casual greetings trigger instant greeting response and reset to idle without unnecessary DAG overhead."
  artifacts:
    - path: ".agents/core/kernel.cjs"
      provides: "Modular AI orchestration lifecycle kernel"
    - path: ".agents/core/event_bus.cjs"
      provides: "Decoupled async event dispatcher for UI and IPC"
    - path: ".agents/registry/brain_registry.cjs"
      provides: "Manifest-driven brain auto-discovery"
    - path: ".agents/dag/graph_planner.cjs"
      provides: "Task decomposition into dependency DAGs"
    - path: ".agents/dag/graph_executor.cjs"
      provides: "DAG traversal and multi-brain handoffs"
    - path: ".agents/adapters/codex_pool_adapter.cjs"
      provides: "Adapter wrapping Codex execution pool"
  key_links:
    - from: ".agents/core/kernel.cjs"
      to: ".agents/registry/brain_registry.cjs"
      via: "loads dynamic brain definitions"
      pattern: "getBrainRegistry"
    - from: ".agents/core/kernel.cjs"
      to: ".agents/dag/graph_planner.cjs"
      via: "plans task DAG"
      pattern: "planGraph"
---

<objective>
Refactor FAIS Brains into a scalable, layered subsystem architecture with dynamic persona auto-discovery and DAG-driven multi-agent task execution.

Purpose: Eliminate monolithic script coupling, enable future brain & tool extensions effortlessly, and power structured multi-brain task handoffs while preserving 100% backward compatibility.
Output: Decoupled `.agents/core/`, `.agents/registry/`, `.agents/dag/`, `.agents/adapters/`, enriched manifests, and backward-compatible shims.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-modular-brain-dag/05-CONTEXT.md
@.agents/brain_orchestrator.cjs
@.agents/codex_brain_pool.cjs
@.agents/task_watcher.cjs
</context>

<tasks>

<task type="auto" id="05-01-01">
<name>Create Core Subsystems: Event Bus, State Machine, and Kernel</name>
<files>
.agents/core/event_bus.cjs
.agents/core/state_machine.cjs
.agents/core/kernel.cjs
.agents/adapters/storage_adapter.cjs
</files>
<instructions>
1. Implement `event_bus.cjs` to handle event queuing, thread-safe file lock checks, and async HTTP webhook broadcasts (`/api/brain/stream-thought`, `/api/brain/tasks`, etc.) with graceful error handling.
2. Implement `state_machine.cjs` to track and validate task lifecycle transitions (`queued`, `planning`, `executing_node`, `reviewing`, `completed`, `failed`, `cancelled`).
3. Implement `storage_adapter.cjs` to encapsulate all IPC read/write operations for `current_brief.json`, `pending_task.json`, and `speaking.lock`.
4. Implement `kernel.cjs` to orchestrate task reception, routing, lifecycle coordination, and clean shutdown.
</instructions>
<verify>
Run Node integration test validating Event Bus emissions and Kernel lifecycle initialization.
</verify>
</task>

<task type="auto" id="05-01-02">
<name>Build Manifest-Driven Brain & Skill Registry</name>
<files>
.agents/registry/brain_registry.cjs
.agents/registry/skill_registry.cjs
.agents/brains/architect.agent.md
.agents/brains/senior_dev.agent.md
.agents/brains/junior_dev.agent.md
.agents/brains/security.agent.md
</files>
<instructions>
1. Update all `.agent.md` files in `.agents/brains/` with structured YAML frontmatter containing:
   - `id`, `name`, `role`, `color`, `avatar`
   - `domain_keywords` (array of domain terms)
   - `collaboration_keywords`
   - `lead_capable` (boolean)
   - `model_tier` (`heavy` / `standard` / `fast`)
   - `system_prompt` (body)
2. Implement `brain_registry.cjs` to scan `.agents/brains/` dynamically on startup, parse frontmatter + markdown body, and provide lookup APIs (`getBrain(id)`, `matchBrainsForTask(text)`, `getAllBrains()`, `getLeadCandidates(text)`).
3. Implement `skill_registry.cjs` to discover and bind custom skills/tools to brains.
</instructions>
<verify>
Execute script verifying that all 4 brains are discovered dynamically with correct domain match scores.
</verify>
</task>

<task type="auto" id="05-01-03">
<name>Implement Dynamic Task Graph (DAG) Planner and Executor</name>
<files>
.agents/dag/task_graph.cjs
.agents/dag/graph_planner.cjs
.agents/dag/graph_executor.cjs
.agents/adapters/codex_pool_adapter.cjs
</files>
<instructions>
1. Implement `task_graph.cjs` providing the in-memory TaskDAG data structure with node addition, dependency edges, topological sort, and execution state.
2. Implement `graph_planner.cjs`:
   - Categorize intent (Casual Greeting, Simple Query, Single-Brain Execution, Complex Multi-Brain Task).
   - Casual Greeting: 0-node immediate response.
   - Simple Actionable: 1-node direct execution.
   - Complex/Architectural Task: Multi-node DAG (e.g. Plan [Architect] -> Build [Senior_Dev/Junior_Dev] -> Security Review [Security] -> Verification [Senior_Dev]).
3. Implement `codex_pool_adapter.cjs` to interface with the existing Codex brain pool runner (`codex_brain_pool.cjs`) and OpenAI completion routines.
4. Implement `graph_executor.cjs` to iterate through ready nodes, invoke assigned brains via adapter, propagate context artifacts from parent nodes to child nodes, and emit stage milestone events via `event_bus.cjs`.
</instructions>
<verify>
Test planning and executing a 3-node mock DAG, verifying node state handoffs and event emissions.
</verify>
</task>

<task type="auto" id="05-01-04">
<name>Wire Facade Shims and Maintain Backward Compatibility</name>
<files>
.agents/brain_orchestrator.cjs
.agents/speak.cjs
.agents/stream_thoughts.js
.agents/finish_task.js
.agents/task_watcher.cjs
</files>
<instructions>
1. Refactor `brain_orchestrator.cjs` to delegate to `kernel.cjs` while preserving CLI interface and exports.
2. Update `speak.cjs`, `stream_thoughts.js`, and `finish_task.js` to dispatch through `event_bus.cjs` directly or via Kernel.
3. Update `task_watcher.cjs` to invoke `kernel.cjs.dispatchTask()` with graceful lease handling and cancellation support.
4. Ensure all existing rules in `AGENTS.md` and CLI commands continue operating seamlessly without breaking.
</instructions>
<verify>
Verify `node .agents/speak.cjs Senior_Dev "Test message"` and `node .agents/finish_task.js "Done"` execute correctly.
</verify>
</task>

<task type="auto" id="05-01-05">
<name>End-to-End Verification and Documentation Update</name>
<files>
ARCHITECTURE.md
INSTALLATION.md
.planning/STATE.md
</files>
<instructions>
1. Run full PHPUnit test suite: `php artisan test --filter=Brain`.
2. Test both casual greeting ("hello team") and actionable prompt workflows.
3. Update `ARCHITECTURE.md` and `INSTALLATION.md` with the new modular subsystem architecture, directory tree, and DAG execution flow.
4. Update `.planning/STATE.md` with Phase 5 completion record.
</instructions>
<verify>
All unit tests pass and documentation accurately reflects the modular subsystem architecture.
</verify>
</task>

</tasks>
