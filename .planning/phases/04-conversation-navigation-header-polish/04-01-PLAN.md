---
phase: 04-conversation-navigation-header-polish
plan: 01
type: execute
wave: 1
depends_on: ["03-caveman-prompt-transport"]
files_modified:
  - resources/js/Pages/Brains/JarvisUI.tsx
autonomous: true
requirements: ["UI-01"]
user_setup:
  - "Manual browser validation at the documented sidebar and viewport widths."
estimate:
  tokens: 10000
  raw_tokens: 5000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "The sidebar header visibly names the selected conversation without opening the chat disclosure."
    - "Tabs, chat disclosure, new-chat action, clear-view action, and connection state are keyboard-operable and expose their state/name."
    - "The header remains usable without overlap or horizontal clipping when the resizable sidebar is 340, 440, or 800 px wide."
  artifacts:
    - path: "resources/js/Pages/Brains/JarvisUI.tsx"
      provides: "Typed, accessible, responsive conversation navigation header"
      contains: "aria-expanded"
  key_links:
    - from: "resources/js/Pages/Brains/JarvisUI.tsx"
      to: "/api/brain/conversations/{conversation}/messages"
      via: "selected conversation object drives the unchanged existing message-load request"
      pattern: "switchConversation"
    - from: "resources/js/Pages/Brains/JarvisUI.tsx"
      to: "/api/brain/conversations"
      via: "New chat uses the existing create endpoint, then sets that response as active"
      pattern: "createConversation"
---

<objective>
Polish the conversation navigation header without changing conversation APIs, persistence, or Thought Stream/Memory Vault semantics.

Purpose: Make the selected chat, navigation actions, and connection state immediately understandable, reliably keyboard accessible, and resilient to the sidebar's real available width.
Output: One focused `JarvisUI.tsx` refinement plus automated build/API-regression evidence and a repeatable manual accessibility/resize matrix.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-conversation-navigation-header-polish/04-CONTEXT.md
@.planning/phases/04-conversation-navigation-header-polish/04-RESEARCH.md
@resources/js/Pages/Brains/JarvisUI.tsx:177-183
@resources/js/Pages/Brains/JarvisUI.tsx:420-474
@resources/js/Pages/Brains/JarvisUI.tsx:722-827
@tests/Feature/BrainConversationTest.php
</context>

<constraints>
- Preserve the existing uncommitted command-terminal styling in `JarvisUI.tsx`; this phase owns only the conversation header and its local interaction wiring.
- Do not add a frontend test framework, dependency, backend endpoint, schema migration, or connection-lifecycle behavior.
- Keep native button/disclosure semantics. Do not claim `menu`, `listbox`, or ARIA tab semantics unless their complete keyboard model is implemented.
- Keep light and dark themes, Thought Stream, Memory Vault, memory count, Clear View, conversation selection, and New Chat behavior.
</constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Make active conversation identity deterministic and visible</name>
  <files>resources/js/Pages/Brains/JarvisUI.tsx</files>
  <read_first>resources/js/Pages/Brains/JarvisUI.tsx:177-183,420-474,790-827, .planning/phases/04-conversation-navigation-header-polish/04-RESEARCH.md:31-51</read_first>
  <behavior>
    - Initial conversation loading shows the first returned conversation title after its message history loads.
    - Selecting a dropdown conversation updates the rendered title and keeps the existing message-history request.
    - Creating a chat sets that returned chat as active immediately, despite React's asynchronous `setConversations` update.
  </behavior>
  <action>Introduce a narrow `Conversation` interface covering the values used by this page (`id`, `title`, `is_history`, and optional count/timestamps if read). Replace conversation `any` state/handler types. Change `switchConversation` to receive the selected `Conversation` object (or explicitly assign it from the successfully selected response), fetch only by its id, and set `activeConversation` from that object after successful loading. Update initial fetch, dropdown selection, and create-chat flow to pass the known object rather than reading the stale `conversations` closure. In the closed conversation disclosure, render a stable “Current chat” label and the active title; use an explicit fallback while data is unresolved and provide the complete title to assistive technology when visual truncation occurs. Preserve endpoint paths, ordering, message mapping, and error handling.</action>
  <verify>
    <automated>npm.cmd run build</automated>
    <manual>Reload with at least three conversations, select each one, then create a new chat; after every action the closed disclosure shows the correct active title and the message stream matches it.</manual>
  </verify>
  <acceptance_criteria>
    - No conversation state or handler in this flow is typed as `any`.
    - `activeConversation` is set from the selected object, not by searching a possibly stale state array.
    - Long and unbroken titles cannot widen the header past its available width.
    - The phase does not change requests to `/api/brain/conversations` or `/api/brain/conversations/{id}/messages`.
  </acceptance_criteria>
  <done>The selected chat is deterministic and visible before any semantic or layout polish is added.</done>
</task>

<task type="auto">
  <name>Task 2: Make all header controls accessible native controls</name>
  <files>resources/js/Pages/Brains/JarvisUI.tsx</files>
  <read_first>resources/js/Pages/Brains/JarvisUI.tsx:751-827, .planning/phases/04-conversation-navigation-header-polish/04-RESEARCH.md:53-77</read_first>
  <behavior>
    - Every header action has a visible focus indication and an accessible name/state.
    - Enter/Space opens the chat disclosure; Escape closes it and returns focus to its trigger.
    - The active choice is identified programmatically and visually without relying on color; New Chat prevents duplicate activation while busy.
  </behavior>
  <action>Keep native buttons and implement the smallest complete disclosure behavior: `type="button"`, an accessible label, `aria-expanded`, `aria-controls`, a ref to restore focus, Escape handling, outside-click close, and focus to the active (or first) conversation only when the disclosure opens. Mark the active conversation with `aria-current="true"` plus a non-color visual marker. Give New Chat a 36–40 px minimum target, `aria-label`, disabled/busy feedback, and focus-visible styles. Give Clear View an explicit accessible name. Retain ordinary buttons for Thought Stream and Memory Vault rather than adding partial ARIA tab roles; style their selected state visually and expose it with an appropriate pressed/current state. Render “Connected” as text next to an `aria-hidden` status dot and disable the pulse under reduced-motion. Do not infer actual transport connectivity; the existing always-connected display remains intentionally unchanged.</action>
  <verify>
    <automated>npm.cmd run build</automated>
    <manual>In each theme, Tab through the header; activate tabs and actions with keyboard, open/close the chat disclosure with Enter/Space/Escape, confirm focus return, then inspect the accessibility tree for labels, expanded/current/busy states, and announced Connected text.</manual>
  </verify>
  <acceptance_criteria>
    - No header control depends only on `title` or color for its accessible name/state.
    - Escape never leaves focus in a hidden disclosure.
    - Conversation options remain ordinary tab-reachable buttons; no incomplete composite-widget behavior is introduced.
    - New Chat, chat disclosure, and other compact controls meet the practical 36–40 px target floor.
  </acceptance_criteria>
  <done>The header is operable and understandable with keyboard and accessibility tree alone.</done>
</task>

<task type="auto">
  <name>Task 3: Apply intrinsic responsive hierarchy and verify regression boundaries</name>
  <files>resources/js/Pages/Brains/JarvisUI.tsx</files>
  <read_first>resources/js/Pages/Brains/JarvisUI.tsx:722-827, .planning/phases/04-conversation-navigation-header-polish/04-RESEARCH.md:79-121, tests/Feature/BrainConversationTest.php</read_first>
  <behavior>
    - Tab controls and conversation/session controls form distinct, compact groups rather than one crowded shallow row.
    - The active chat remains readable, dropdown stays within the sidebar, and controls do not overlap at sidebar widths 340, 440, and 800 px.
    - Both themes retain clear selected/current, hover, focus, disabled, and reduced-motion states.
  </behavior>
  <action>Restructure only the header into intrinsic flex/grid groups using `min-w-0`, truncation, wrapping, and available-width-aware dropdown sizing; avoid relying solely on viewport breakpoints because the desktop sidebar is resizable. Keep the primary tab group distinct from conversation/session actions, while keeping normal titles compact enough to avoid a third header line. Pair dark/light color classes for every changed state, preserve the memory badge and conditional Clear View placement, and constrain the dropdown to the sidebar control width. Do not change the stream, memory panel content, resize limits, task composer, API contracts, or conversation storage. Run the focused Laravel API suite after UI work to prove conversation list/create/isolation behavior remains available, then run the production build and whitespace check.</action>
  <verify>
    <automated>php artisan test --filter=BrainConversationTest; npm.cmd run build; git diff --check</automated>
    <manual>Test both themes with empty, History, busy new chat, memory-count, and Clear View states; use short, long, and unbroken titles at 340/440/800 px sidebar widths and 320/375/768 px viewport widths. Confirm no overflow, overlap, clipping, unreachable controls, or dropdown escape; capture default, 340 px, and mobile before/after evidence.</manual>
  </verify>
  <acceptance_criteria>
    - The header works at every documented sidebar/viewport width without horizontal overflow or inaccessible controls.
    - Active tab and active conversation remain visible at the narrowest intended size.
    - `php artisan test --filter=BrainConversationTest`, `npm.cmd run build`, and `git diff --check` pass.
    - No backend, persistence, API, or frontend-test-harness file is modified.
  </acceptance_criteria>
  <done>UI-01 is complete with responsive hierarchy, theme parity, accessible controls, and documented automated/manual evidence.</done>
</task>

</tasks>

<success_criteria>
  <criterion id="UI-01-1">Users can identify the active tab and active chat at a glance, without opening the chat selector.</criterion>
  <criterion id="UI-01-2">Every header control has an accessible name, visible focus treatment, and keyboard-usable interaction; disclosure focus behavior is deterministic.</criterion>
  <criterion id="UI-01-3">The header remains usable at 340/440/800 px sidebar widths and 320/375/768 px mobile viewport widths in both themes.</criterion>
  <criterion id="UI-01-4">Conversation list/create/history behavior remains regression-checked by the existing Laravel feature suite.</criterion>
</success_criteria>

<final_verification>
<automated>php artisan test --filter=BrainConversationTest; npm.cmd run build; git diff --check</automated>
<manual>Execute the Task 3 keyboard/accessibility and responsive matrix in both themes, including Escape/focus return and reduced-motion; retain the requested screenshots as evidence.</manual>
</final_verification>
