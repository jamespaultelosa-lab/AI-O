---
phase: 03-caveman-prompt-transport
plan: 01
type: execute
wave: 1
depends_on: ["02-dynamic-persona-stream"]
files_modified:
  - resources/js/lib/cavemanPromptTransport.ts
  - resources/js/Pages/Brains/JarvisUI.tsx
  - tests/Node/caveman_prompt_transport.test.cjs
autonomous: true
requirements: ["TOKEN-01"]
---

<objective>
Create the client-side dual-message contract: complete text remains visible while a deterministic Caveman-compressed copy is prepared for backend transport.

Purpose: reduce model-input tokens without making the UI, history, or user acknowledgement cryptic or lossy.
</objective>

<tasks>

<task type="tracer">
  <name>Task 1: Implement a safe deterministic Caveman prompt compressor</name>
  <files>resources/js/lib/cavemanPromptTransport.ts, tests/Node/caveman_prompt_transport.test.cjs</files>
  <action>Export typed `buildCavemanPromptTransport(displayTask)` returning `{ displayTask, compressedTask }`. Tokenize protected regions before prose compaction, then restore them exactly. Protected regions include fenced and inline code, URLs, filesystem paths, CLI commands, numeric/version values, environment variables, and image markers. Compact only ordinary prose using a small, documented Caveman rule set (whitespace normalization and removal of a finite filler phrase list); do not invent abbreviations, alter negation, or claim semantic equivalence. Return the original unchanged if safe tokenization cannot complete.</action>
  <verify><automated>node --test tests/Node/caveman_prompt_transport.test.cjs</automated></verify>
  <done>Compressor is deterministic, typed, and preserves every protected literal byte-for-byte in its compressed output.</done>
</task>

<task type="auto">
  <name>Task 2: Send both display and transport representations from the UI</name>
  <files>resources/js/Pages/Brains/JarvisUI.tsx, resources/js/lib/cavemanPromptTransport.ts, tests/Node/caveman_prompt_transport.test.cjs</files>
  <action>At submit time, derive the dual representation once from the text-area value. Continue appending only `displayTask` to the visible `USER` message list, including existing attachment presentation. Post `{ display_task: displayTask, task: compressedTask, images }` to `/api/brain/dispatch`. Keep the full display text in local state when a request fails so retry/error UI remains intelligible. Add a focused test or extracted pure submission-payload helper proving the two fields are distinct for compactable prose and that the UI field retains the original input.</action>
  <verify><automated>npm run build</automated></verify>
  <done>The browser keeps and displays the complete user request while submitting a separate compact task field.</done>
</task>

</tasks>

<success_criteria>
  <criterion>The visible user message is byte-for-byte the submitted display text, apart from the existing attachment rendering.</criterion>
  <criterion>Compactable prose produces a distinct `task` transport value.</criterion>
  <criterion>Protected literals remain exact within the compacted transport value.</criterion>
</success_criteria>

