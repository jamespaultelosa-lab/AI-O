---
phase: 03-caveman-prompt-transport
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/Http/Controllers/TaskDispatcherController.php
  - .agents/task_watcher.cjs
  - .agents/brain_orchestrator.cjs
  - tests/Feature/TaskPromptTransportTest.php
  - tests/Node/brain_orchestrator.test.cjs
autonomous: true
requirements: ["TOKEN-01"]
---

<objective>
Carry the compact task representation through Laravel IPC and Codex orchestration while keeping the original representation exclusively for the user-facing record.

Purpose: enforce the dual-message boundary end to end without allowing compression to alter routing or UI history.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Validate and separate display and transport fields at dispatch</name>
  <files>app/Http/Controllers/TaskDispatcherController.php, tests/Feature/TaskPromptTransportTest.php</files>
  <action>Require validated non-empty `display_task` and `task` string fields for the new dispatch contract, with independent maximum lengths and pre-side-effect validation. Use `display_task` for task-intent discernment, user message persistence, logs/acknowledgement, and response echo. Use compressed `task` only to construct the queue/pending payload. Replace ambiguous `raw_task` with explicit `display_task` and `transport_task` fields. Keep image URLs as attachment metadata appended only at the execution boundary, not in the display record. Add a temporary backwards-compatibility decision only if existing non-UI clients are identified; otherwise fail closed rather than silently treating raw text as compressed.</action>
  <verify><automated>php artisan test --filter=TaskPromptTransportTest</automated></verify>
  <done>Controller validates both fields, keeps the original visible, and places only the compressed representation in execution IPC.</done>
</task>

<task type="auto">
  <name>Task 2: Consume only compressed transport text in watcher and orchestrator</name>
  <files>.agents/task_watcher.cjs, .agents/brain_orchestrator.cjs, tests/Node/brain_orchestrator.test.cjs</files>
  <action>Update queue/pending normalization for the explicit `transport_task` schema. Route using `display_task` when needed for intent/context, but build every `buildIdentity`, consultant, lead, and final-synthesis model prompt from `transport_task` plus attachment metadata only. Do not include the original display text in prompt context, prior-task state, webhook status, or error output. Preserve decision-follow-up handling by storing the compact version as previous execution context and retain the original only as UI/audit metadata.</action>
  <verify><automated>node --test tests/Node/brain_orchestrator.test.cjs</automated></verify>
  <done>Watcher-to-Codex execution never receives the full display text, while routing and UI semantics retain it.</done>
</task>

</tasks>

<success_criteria>
  <criterion>Feature tests prove `display_task` appears in user history and the acknowledgement, while `transport_task` is the IPC task payload.</criterion>
  <criterion>Node tests prove prompts contain compressed task text and omit a unique original-only phrase.</criterion>
  <criterion>Malformed or missing either representation produces no persisted message, IPC file mutation, or broadcast.</criterion>
</success_criteria>

<final_verification>
<automated>node --test tests/Node/caveman_prompt_transport.test.cjs tests/Node/brain_orchestrator.test.cjs &amp;&amp; php artisan test --filter=TaskPromptTransportTest &amp;&amp; npm run build &amp;&amp; git diff --check</automated>
<manual>Submit a prose-heavy request containing a URL and inline code. Confirm the UI/history show the complete original request and the watcher debug fixture/model prompt contains the compressed task with the URL and code intact.</manual>
</final_verification>
